const chart=document.querySelector('[data-modalidades-chart]');if(chart){const source=JSON.parse(chart.dataset.modalidadesChart||'[]');if(source.length&&window.Chart){new Chart(chart,{type:'bar',data:{labels:source.map(item=>item.nome),datasets:[{label:'Atletas',data:source.map(item=>item.total),backgroundColor:'#16A37A',borderRadius:7}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{beginAtZero:true,ticks:{precision:0}}}}})}else{chart.closest('.chart-wrap').innerHTML='<div class="empty-state"><h2>Sem dados suficientes</h2><p>Os dados por modalidade aparecerão aqui quando houver vínculos cadastrados.</p></div>'}}
