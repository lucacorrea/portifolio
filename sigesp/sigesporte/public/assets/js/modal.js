let opener=null;document.querySelectorAll('[data-modal-open]').forEach(button=>button.addEventListener('click',()=>{opener=button;const modal=document.querySelector(button.dataset.modalOpen);if(modal){modal.hidden=false;modal.querySelector('button')?.focus()}}));document.querySelectorAll('[data-modal-close]').forEach(button=>button.addEventListener('click',()=>{const modal=button.closest('.modal');if(modal){modal.hidden=true;opener?.focus()}}));document.addEventListener('keydown',event=>{if(event.key==='Escape')document.querySelectorAll('.modal:not([hidden])').forEach(modal=>modal.hidden=true)});
