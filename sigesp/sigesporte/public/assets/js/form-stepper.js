document.querySelectorAll('[data-stepper]').forEach(stepper=>{const buttons=[...stepper.querySelectorAll('[data-step]')],panels=[...document.querySelectorAll('[data-step-panel]')];const activate=index=>{buttons.forEach((button,i)=>button.classList.toggle('is-active',i===index));panels.forEach((panel,i)=>panel.hidden=i!==index)};buttons.forEach((button,index)=>button.addEventListener('click',()=>activate(index)));activate(0)});
