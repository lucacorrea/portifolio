const region=document.querySelector('[data-toast-region]');const show=message=>{if(!region)return;const toast=document.createElement('div');toast.className='toast';toast.textContent=message;region.append(toast);setTimeout(()=>toast.remove(),4200)};document.querySelectorAll('[data-toast]').forEach(button=>button.addEventListener('click',event=>{if(button.tagName==='A')event.preventDefault();show(button.dataset.toast)}));window.sigespToast=show;
