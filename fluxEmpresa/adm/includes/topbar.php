<?php declare(strict_types=1); $activeCompany = $application->activeCompanyContext()->current(); ?>
<header class="admin-topbar"><button class="btn btn-light" type="button" data-sidebar-toggle aria-label="Abrir menu"><i class="bi bi-list"></i></button><div><p class="admin-breadcrumb">Administração / <?= admin_h($pageTitle ?? '') ?></p><h1><?= admin_h($pageTitle ?? '') ?></h1><span><?= admin_h($pageSubtitle ?? '') ?></span></div><div class="admin-user"><span><?= admin_h($currentUser->profileName()) ?> · <?= admin_h($currentUser->profileCode()) ?></span><strong><?= admin_h($currentUser->name()) ?></strong></div></header>
<?php if ($activeCompany): ?><div class="support-banner"><i class="bi bi-shield-check"></i>Atendimento administrativo — <?= admin_h($activeCompany->name) ?><a href="<?= admin_url('empresa.php?id=' . $activeCompany->id) ?>">Voltar aos detalhes</a><form method="post" action="<?= admin_url('actions/empresa-sair.php') ?>"><?= $csrf->field() ?><button type="submit">Encerrar atendimento</button></form></div><?php endif; ?>
